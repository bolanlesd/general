# Example sops-encrypted secrets file (plaintext shape only).
# Run `sops -e -i secrets.enc.yaml` to encrypt before committing.
aws:
  access_key_id: AKIA-EXAMPLE
  secret_access_key: example-secret-do-not-commit-plaintext
github:
  token: ghp_exampletokenvalue
